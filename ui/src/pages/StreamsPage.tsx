export default function StreamsPage() {
  return (
    <section className="panel">
      <h2>Streams</h2>
      <p>
        Clones e mocks do seu workspace (Stream Mock). Implementação na Fase 3
        (orquestrador → Mock); o dashboard standalone do Mock continua
        disponível para uso de dev/QA.
      </p>
    </section>
  );
}
